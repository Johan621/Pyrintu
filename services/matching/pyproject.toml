[project]
name = "pyrintu-matching"
version = "0.1.0"
description = "Pyrintu Matching Service - Pairwise activity-based matching with deterministic compatibility"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.pytest.ini_options]
testpaths = ["tests"]
